<%= form_for(bookseries) do |f| %>
  <% if bookseries.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookseries.errors.count, "error") %> prohibited this bookseries from being saved:</h2>

      <ul>
      <% bookseries.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %>
    <%= f.text_field :book_id %>
  </div>

  <div class="field">
    <%= f.label :series_id %>
    <%= f.text_field :series_id %>
  </div>

  <div class="field">
    <%= f.label :position %>
    <%= f.number_field :position %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
