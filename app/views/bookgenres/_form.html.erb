<%= form_for(bookgenre) do |f| %>
  <% if bookgenre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookgenre.errors.count, "error") %> prohibited this bookgenre from being saved:</h2>

      <ul>
      <% bookgenre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %>
    <%= f.text_field :book_id %>
  </div>

  <div class="field">
    <%= f.label :genre_id %>
    <%= f.text_field :genre_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
