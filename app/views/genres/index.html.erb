<p id="notice"><%= notice %></p>

<h1>Genres</h1>

<ul id="genres">
    <% @genres.each do |genre| %>
      <li>
        <%= link_to genre.name, genre %>
      </li>
    <% end %>
</ul>

<br>

<%= link_to 'New Genre', new_genre_path %>
